public class Car {

    long
            currentX = 0,
            currentY = 0,
            currentStep = 0;

    void take(Ride ride) {
        currentX = ride.endX;
        currentY = ride.endY;
        currentStep = // TODO
    }
}
